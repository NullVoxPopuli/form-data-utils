{
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:NullVoxPopuli/form-data.git"
  },
  "license": "MIT",
  "author": "",
  "scripts": {
    "build": "pnpm --filter form-data-utils build",
    "lint": "pnpm --filter '*' lint",
    "lint:fix": "pnpm --filter '*' lint:fix",
    "prepare": "pnpm build",
    "test": "pnpm --filter '*' test",
    "test:ember": "pnpm --filter '*' test:ember"
  },
  "devDependencies": {
    "@glint/core": "^1.2.1",
    "concurrently": "^9.0.0",
    "prettier": "^3.0.3",
    "prettier-plugin-ember-template-tag": "^2.0.2",
    "release-plan": "^0.9.0"
  }
}
